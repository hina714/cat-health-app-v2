# QAレポート 2026-03-23

## 確認した実装

- `src/app/api/cats/route.ts`
- `src/app/api/cats/[id]/route.ts`
- `src/lib/db.ts`

## テスト結果

> サーバーが未起動のため、コードレビューのみ実施。

### GET /api/cats
- 実装: `SELECT * FROM cats ORDER BY created_at DESC`
- レスポンス形式: `{ ok: true, cats: [...] }`
- 結果: ✅ 問題なし

### POST /api/cats
- 必須パラメータ: `name`, `user_id`
- 不足時: 400エラーを返す
- 成功時: 201 + `{ ok: true, cat: {...} }`
- 結果: ✅ 問題なし

### GET /api/cats/[id]
- 存在しないIDの場合: 404を返す
- 結果: ✅ 問題なし

### PATCH /api/cats/[id]
- 送ったフィールドだけ更新（`COALESCE` による部分更新）
- 存在しないIDの場合: 404を返す
- 結果: ✅ 問題なし

### DELETE /api/cats/[id]
- 成功時: 204 No Content
- 存在しないIDの場合: 404を返す
- 結果: ✅ 問題なし

## 問題点・改善提案

### 問題1: DBエラーが500として素通りする
- 対象ファイル: `src/app/api/cats/route.ts`, `src/app/api/cats/[id]/route.ts`
- 内容: try/catch がないため、DB接続エラーや予期しないエラー発生時にNext.jsのデフォルト500レスポンスが返る
- 提案: 現段階では許容範囲。本番前に try/catch を追加してエラーメッセージを制御する

### 問題2: user_id が外部から自由に指定できる
- 対象ファイル: `src/app/api/cats/route.ts`
- 内容: 認証未実装のため、誰でも任意の user_id で猫を登録できる状態
- 提案: TODO コメントあり。認証実装時に session から取得するよう変更する（想定済み）

## 総評

コードの構造は `src/app/api/test/route.ts` のパターンと統一されており、一貫性がある。
既知の制約（user_id の仮実装）は TODO コメントで明示されている。
サーバー起動後に実際のAPIリクエストで動作確認を行うことを推奨する。
